- # app/views/role_events/edit.html.haml

%h2= present(@role).label

- presenter = present(@event)
%h3= "Edit #{presenter.type} Event"

= render :partial => "role_events/forms/#{@event._type.underscore}_form", :locals => { :role => @role, :event => @event, :presenter => presenter }
